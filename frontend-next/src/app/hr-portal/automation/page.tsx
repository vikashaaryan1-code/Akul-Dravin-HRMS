'use client'; import React, { useState } from 'react';
import { DocumentGeneratorWidget } from '../../../components/automation/DocumentGeneratorWidget';
import { DocumentPreviewCard } from '../../../components/automation/DocumentPreviewCard'; export default function AutomationDashboardPage() { const [docContent, setDocContent] = useState(''); const [docType, setDocType] = useState('Awaiting Generation'); const handleDocumentGenerated = (type: string, content: string) => { setDocType(type.replace('_', ' ')); setDocContent(content); }; return ( <div className="p-8 max-w-7xl mx-auto min-h-screen relative"> <div className="relative z-10 mb-10 pb-6 border-b border-white/10"> <h1 className="text-4xl font-bold text-navy mb-2">AI Document Factory</h1> <p className="text-white/60 text-sm max-w-2xl"> Powered by AiAutomationEngineService (Layer 8). Synthesize structurally perfect, legally sound HR documentation in seconds. </p> </div> <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 relative z-10"> {/* Controls */} <div className="animate-fade-in"style={{ animationDelay: '0ms' }}> <DocumentGeneratorWidget onGenerate={handleDocumentGenerated} /> </div> {/* Preview */} <div className="animate-fade-in"style={{ animationDelay: '150ms' }}> <DocumentPreviewCard content={docContent} documentType={docType} /> </div> </div> </div> );
}
