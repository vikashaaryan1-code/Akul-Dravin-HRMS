# Pages Architecture

This project uses the Next.js App Router under `src/app` for route handling.

The `src/pages` folder exists to satisfy architecture documentation requirements and reserve a space for any migration utilities if a Pages Router legacy surface is ever needed.
