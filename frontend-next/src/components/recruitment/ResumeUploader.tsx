'use client'; import React, { useState } from 'react';
import { ThreeDGlassCard } from '../ui/ThreeDGlassCard';
import { GlassCard } from '../ui/GlassCard'; export const ResumeUploader = ({ onResumeParsed }: { onResumeParsed: (data: any) => void }) => { const [isDragging, setIsDragging] = useState(false); const [isProcessing, setIsProcessing] = useState(false); const simulateParse = async () => { setIsProcessing(true); /* Simulate AI parsing delay */ await new Promise(resolve => setTimeout(resolve, 2000)); /* Simulate API response from /api/v1/ai/recruitment/parse-resume */ const mockData = { name:"Alex Candidate", email:"alex@example.com", skills: ["React","NestJS","TypeScript","AWS"], experience: [ { company:"Tech Corp", position:"Senior Engineer", duration:"3 years"} ] }; setIsProcessing(false); onResumeParsed(mockData); }; return ( <GlassCard className={`relative p-8 border-2 border-dashed transition-all duration-300 ${ isDragging ? 'border-primary/50 bg-primary/10' : 'border-white/20 hover:border-white/40' }`} > <div className="flex flex-col items-center justify-center space-y-4"onDragOver={(e) => { e.preventDefault(); setIsDragging(true); }} onDragLeave={() => setIsDragging(false)} onDrop={(e) => { e.preventDefault(); setIsDragging(false); simulateParse(); }} > <div className="p-4 rounded-full bg-white/5"> <svg className="w-8 h-8 text-white/70"fill="none"viewBox="0 0 24 24"stroke="currentColor"> <path strokeLinecap="round"strokeLinejoin="round"strokeWidth={2} d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"/> </svg> </div> <div className="text-center"> <p className="text-lg font-medium text-white"> {isProcessing ? 'AI is extracting data...' : 'Drag & Drop Resume'} </p> <p className="text-sm text-white/50 mt-1"> or click to browse (PDF, DOCX) </p> </div> {!isProcessing && ( <button onClick={simulateParse} className="px-6 py-2 bg-primary/20 hover:bg-primary/40 border border-primary/30 rounded-full text-white transition-all"> Simulate Raw Text Paste </button> )} </div> {isProcessing && ( <div className="absolute inset-0 bg-background/80 backdrop-blur-sm flex items-center justify-center rounded-2xl"> <div className="w-8 h-8 border-4 border-primary border-t-transparent rounded-full animate-spin"></div> </div> )} </GlassCard> );
};
