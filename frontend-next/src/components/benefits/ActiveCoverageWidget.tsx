'use client'; import React, { useEffect, useState } from 'react'; type CoverageData = { type: string; provider: string; plan: string; tier: string; cost: number }; export const ActiveCoverageWidget = () => { const [data, setData] = useState<CoverageData[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { fetch('/api/benefits/coverage') .then(res => res.ok ? res.json() : []) .then((d: CoverageData[]) => { setData(Array.isArray(d) ? d : []); setLoading(false); }) .catch(() => setLoading(false)); }, []); return ( <div className="bg-[#0f172a]/80 backdrop-blur-xl border border-white/10 p-6 rounded-2xl shadow-2xl h-full relative overflow-hidden"> <div className="flex justify-between items-start mb-6 z-10 relative"> <div> <h3 className="text-white text-xl font-bold mb-1">Active Coverage</h3> <p className="text-white/50 text-sm">Your current health insurance elections</p> </div> <button className="px-4 py-2 bg-white/5 hover:bg-white/10 text-white rounded text-sm transition-colors border border-white/10"> View ID Cards </button> </div> <div className="space-y-4 z-10 relative"> {loading ? ( <div className="animate-pulse space-y-4"> <div className="h-24 bg-white/10 rounded-xl w-full"></div> <div className="h-24 bg-white/10 rounded-xl w-full"></div> <div className="h-24 bg-white/10 rounded-xl w-full"></div> </div> ) : ( data.map((item, idx) => ( <div key={idx} className="bg-black/40 border border-white/5 p-4 rounded-xl hover:border-white/20 transition-colors"> <div className="flex justify-between items-center mb-3"> <div className="flex items-center gap-3"> <div className="w-10 h-10 rounded-full bg-teal-500/20 flex items-center justify-center border border-teal-500/30"> <span className="text-teal-400 font-bold text-lg">{item.type[0]}</span> </div> <div> <h4 className="text-white font-semibold">{item.type}</h4> <p className="text-teal-400/80 text-xs">{item.provider}</p> </div> </div> <div className="text-right"> <span className="text-navy font-bold">${item.cost.toFixed(2)}</span> <span className="text-white/40 text-xs block">/ pay period</span> </div> </div> <div className="flex gap-4 pt-3 border-t border-white/5 text-sm"> <div className="flex-1"> <span className="text-white/40 block text-xs mb-1">Plan Name</span> <span className="text-white/80">{item.plan}</span> </div> <div className="flex-1"> <span className="text-white/40 block text-xs mb-1">Coverage Tier</span> <span className="text-white/80">{item.tier}</span> </div> </div> </div> )) )} </div> </div> );
};
