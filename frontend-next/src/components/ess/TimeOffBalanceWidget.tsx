'use client'; import React from 'react';
import { motion } from 'framer-motion'; const BALANCES = [ { type: 'Paid Time Off', used: 10, total: 20, color: 'bg-blue-500' }, { type: 'Sick Leave', used: 2, total: 10, color: 'bg-purple-500' }, { type: 'Floating Holiday', used: 1, total: 2, color: 'bg-green-500' },
]; export const TimeOffBalanceWidget = () => { const handleRequest = () => { alert('Mock: Time Off Request Modal would open here.'); }; return ( <div className="bg-[#0f172a]/80 backdrop-blur-xl border border-white/10 p-6 rounded-2xl shadow-2xl h-full flex flex-col justify-between relative overflow-hidden"> {/* Decorative Gradient */} <div className="absolute -top-24 -right-24 w-64 h-64 bg-blue-500/10 rounded-full blur-3xl pointer-events-none"></div> <div> <h3 className="text-white text-xl font-bold mb-1">Time Off Balances</h3> <p className="text-white/50 text-sm mb-6">Current accrual cycle (Jan 2026 - Dec 2026)</p> <div className="space-y-6"> {BALANCES.map((item, idx) => { const percentage = (item.used / item.total) * 100; const remaining = item.total - item.used; return ( <div key={idx}> <div className="flex justify-between items-end mb-2"> <span className="text-white/80 font-medium">{item.type}</span> <span className="text-navy font-bold text-lg">{remaining} <span className="text-white/40 text-sm font-normal">days left</span></span> </div> <div className="h-2 w-full bg-black/40 rounded-full overflow-hidden"> <motion.div initial={{ width: 0 }} animate={{ width: `${percentage}%` }} transition={{ duration: 1, delay: 0.2 * idx }} className={`h-full ${item.color} rounded-full`} /> </div> </div> ); })} </div> </div> <button onClick={handleRequest} className="mt-8 w-full py-3 bg-white/5 hover:bg-white/10 border border-white/10 text-white font-semibold rounded-xl transition-all duration-300 shadow-[0_0_15px_rgba(59,130,246,0.1)] hover:shadow-[0_0_20px_rgba(59,130,246,0.2)]"> Request Time Off </button> </div> );
};
